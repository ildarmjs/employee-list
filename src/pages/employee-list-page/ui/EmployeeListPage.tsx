import { FC } from 'react'
import { EmployeeList } from '../../../widgets/employee-list'

interface EmployeeListPageProps {}

export const EmployeeListPage: FC<EmployeeListPageProps> = ({}) => {
	return <EmployeeList />
}
